# Local dev API. In production, prefer the API-served app and omit this.
# If a static Render frontend sets this to the API origin, the app redirects
# there before sign-in so browser cookies are first-party.
VITE_API_BASE_URL=http://localhost:5000
BASE_PATH=/
